# Basic Syntax of GUI in Python
from tkinter import *   # Importing all from tkinter module

root = Tk()  # Creating a root window

root.mainloop()  # This is the main loop which keeps the window open